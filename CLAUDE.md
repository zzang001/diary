# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Context

This is Study-05 in a VibeCoding study series. The project is a fresh workspace with an OpenRouter API key configured in `.env` for accessing LLM APIs. No application code exists yet.

## Environment

- **OpenRouter API key** is stored in `.env` as `OPENROUTER_API_KEY`. Use this to call models via the OpenRouter API endpoint (`https://openrouter.ai/api/v1`), which is OpenAI-compatible.
